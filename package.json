{
  "name": "webos-hub",
  "version": "1.0.0",
  "private": true,
  "description": "webOS TV hub app: one home screen over Jellyfin + Sunshine, with deep-links into the installed Jellyfin and Moonlight apps.",
  "scripts": {
    "build": "node tools/build.mjs",
    "smoke": "node tools/smoke-test.mjs",
    "check": "for f in js/*.js services/service.js tools/*.mjs; do node --check \"$f\" || exit 1; done && echo 'syntax OK'"
  },
  "license": "MIT"
}
